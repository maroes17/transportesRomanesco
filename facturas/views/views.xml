<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="facturas.list">
      <field name="name">facturas list</field>
      <field name="model">facturas.facturas</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="facturas.action_window">
      <field name="name">facturas window</field>
      <field name="res_model">facturas.facturas</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="facturas.action_server">
      <field name="name">facturas server</field>
      <field name="model_id" ref="model_facturas_facturas"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="facturas" id="facturas.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="facturas.menu_1" parent="facturas.menu_root"/>
    <menuitem name="Menu 2" id="facturas.menu_2" parent="facturas.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="facturas.menu_1_list" parent="facturas.menu_1"
              action="facturas.action_window"/>
    <menuitem name="Server to list" id="facturas" parent="facturas.menu_2"
              action="facturas.action_server"/>
    -->
  </data>
</odoo>